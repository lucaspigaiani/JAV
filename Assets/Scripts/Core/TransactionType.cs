using System; // Used for standard operations

// Definition of the TransactionType enumeration
public enum TransactionType
{
    Sale,      // Represents a sale transaction
    Donation   // Represents a donation transaction
}